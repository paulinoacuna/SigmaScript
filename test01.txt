variable_1 = 2;

cat a = real_set*2;
variable_1.to_Steak_Model();

model(a_1,t_1,b_1, M_1).to_Steak_Model();

array_1 = [1, 3.4, 6, 7.8];
a = int_set > 0;

alphabet_1 = [a, b, c];

fun multicotomize_chain(x);